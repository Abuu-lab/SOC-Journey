# 🟢 EVALUASI DAY 8 — PASSED

Sesuai aturan kita:

> **76–100% = PASS**

Jawabanmu **PASS**. Kamu sudah memahami konsep utama Day 8 dan bisa menggabungkan **User + PID + PPID + ExecutablePath + CommandLine**.

Ada beberapa kesalahan kecil dan beberapa istilah yang masih perlu diluruskan. Semuanya **akan kita bawa sebagai reminder ke Day 9**, bukan mengulang Day 8.

---

# 1. PRAKTIK 1 ✅

Kamu mendapatkan:

```text
desktop-c7bhmkl\buya
```

Artinya current security context yang sedang kamu gunakan adalah:

```text
DESKTOP-C7BHMKL\Buya
```

`whoami` ✅ benar.

`whoami /groups` juga berhasil.

Kamu melihat:

```text
Everyone
```

Hari ini belum perlu menghafal semua group yang muncul. Yang penting kamu mengerti bahwa:

> `whoami /groups` menunjukkan group membership/security context dari account tersebut.

---

# 2. PRAKTIK 2 ✅

Kamu mendapatkan:

```text
AggregatorHost     6348   NT AUTHORITY\SYSTEM
ApplicationFrameHost 22496 DESKTOP-C7BHMKL\Buya
atieclxx           22220  NT AUTHORITY\SYSTEM
atiesrxx            3388  NT AUTHORITY\SYSTEM
```

Bagus.

Kamu sekarang sudah bisa menghubungkan:

```text
Process
   +
PID
   +
UserName
```

Ini memang tujuan Day 8.

---

# 3. PRAKTIK 3 ✅

```text
AggregatorHost
PID 6348
UserName NT AUTHORITY\SYSTEM
```

Benar.

---

# 4. PRAKTIK 4 ✅

Kamu mendapatkan:

```text
Name            : AggregatorHost.exe
ProcessId       : 6348
ParentProcessId : 4664
ExecutablePath  : C:\WINDOWS\System32\AggregatorHost.exe
CommandLine     : AggregatorHost.exe
```

Bagus.

Sekarang kamu punya:

```text
User
Process
PID
PPID
ExecutablePath
CommandLine
```

Itu sudah menjadi **basic endpoint context**.

---

# 5. CHALLENGE

## Q1 User ✅

> Buya

Benar berdasarkan kasus yang diberikan.

---

## Q2 Parent Process ✅

> explorer.exe

Benar.

---

## Q3 PPID ✅

> ID milik Parent Process yaitu explorer.exe

✅ Benar.

Lebih presisi:

```text
PPID = PID milik parent process
```

---

## Q4 ExecutablePath ✅

> letak executable berada di System32 yang kelihatannya normal

Bagus.

Tapi hati-hati dengan kata:

> "kelihatannya normal"

Itu adalah **observation**, bukan verdict.

Contoh:

```text
C:\Windows\System32\...
```

→ path terlihat expected/normal.

Tetapi tetap bisa memerlukan verification.

---

## Q5 User sebagai evidence ✅

Jawabanmu bagus:

> account yang digunakan untuk menjalankan process

Betul.

Sekarang pertanyaan investigation kita berkembang dari:

> **What is running?**

menjadi:

> **Who is running it?**

---

## Q6 User = Buya berarti benign? ✅

> belum tentu

Benar.

```text
User = Buya
≠
Benign
```

---

## Q7 PowerShell + Buya otomatis malicious? 🟢

Kamu menjawab:

> bisa jadi, tapi juga belum tentu

Arah berpikirmu benar.

Lebih tepat:

> **Tidak otomatis malicious.**

Kita harus melihat context dan evidence lain.

---

## Q8 Kenapa perlu investigation? ✅

Kamu fokus pada:

```text
-ExecutionPolicy Bypass
```

Benar, itu adalah **suspicious indicator** yang layak diperiksa lebih lanjut.

Tetapi tetap:

```text
Bypass
≠
Malware
```

---

## Q9 Evidence tambahan 🟡

Kamu memberikan:

```text
SHA256
DigitalSignature
NetworkAnalysis
Metadata
```

Itu **4**, padahal diminta minimal 5.

Tetapi ini hanya kesalahan kecil.

Evidence kelima bisa misalnya:

```text
User
Process creation/log telemetry
Parent process context
Threat Intelligence
```

Karena dari kasus User, Parent, PID, PPID sebenarnya sudah diketahui, maka yang paling berguna sebagai tambahan mungkin:

```text
Threat Intelligence / file reputation
Windows Event Logs
Network connections
```

---

## Q10 Perbedaan User / Process / PID / PPID / Privilege ✅

Jawabanmu secara konsep **bagus**.

```text
User      → account/security context
Process   → instance program yang sedang berjalan
PID       → ID current process
PPID      → PID parent process
Privilege → capabilities/permissions yang tersedia bagi security context
```

🟢 Paham.

---

# 6. MINI SOC INVESTIGATION

Kamu memilih:

```text
steamservice
PID: 13480
PPID: 1424
User: NT AUTHORITY\SYSTEM
```

### Q1 User Context 🟡

Kamu menjawab:

> NT AUTHORITY

Kurang lengkap.

Output-mu:

```text
NT AUTHORITY\SYSTEM
```

Jadi jawabannya:

> **NT AUTHORITY\SYSTEM**

Ini penting karena `NT AUTHORITY` bukan nama account lengkap dalam output tersebut; `SYSTEM` adalah account/security principal-nya.

---

### Q2 Parent Process 🟡

Kamu menulis:

> service.exe

Kemungkinan yang kamu maksud adalah `services.exe`.

Di Windows kita sudah belajar:

```text
services.exe
```

bukan:

```text
service.exe
```

Ini kelihatannya sepele, tetapi untuk investigator **nama process harus presisi**.

Aku bawa ini sebagai reminder Day 9.

---

### Q3 ExecutablePath ✅

Benar:

```text
C:\Program Files (x86)\Common Files\Steam\steamservice.exe
```

---

### Q4 Command Context 🟡

Kamu bilang lupa.

CommandLine:

```text
"C:\Program Files (x86)\Common Files\Steam\steamservice.exe" /RunAsService
```

Artinya secara sederhana:

```text
Executable:
steamservice.exe

Argument:
 /RunAsService
```

Jadi:

> **CommandLine menunjukkan bagaimana process tersebut dijalankan, termasuk executable invocation dan arguments.**

Bukan:

```text
service.exe > steamservice > user
```

Yang kamu tulis itu lebih seperti **process relationship**, bukan command context.

---

### Q5 Assessment ✅

Kamu memilih:

> Belum dapat ditentukan

🟢 Ini jawaban yang sangat aman secara investigation.

Kita belum punya cukup evidence untuk memberikan verdict.

---

### Q6 Evidence ✅

Sudah bagus:

```text
PID
PPID
ExecutablePath
CommandLine
User
```

Kemudian:

```text
SHA-256
Digital Signature
Network
Metadata
```

Ini sudah menjadi evidence set yang cukup baik untuk level kita sekarang.

---

# 7. ACTIVE RECALL DAY 1–7

### Q1 Process ✅

> program yang sedang berjalan

Benar untuk level dasar.

---

### Q2 PID ✅

Benar.

---

### Q3 PPID ✅

Benar.

---

### Q4 ExecutablePath vs CommandLine 🟡

Kamu:

> ExecutablePath = letak executable
> CommandLine = ExecutablePath + argument

Yang kedua **hampir**, tetapi jangan menghafalkan sebagai definisi.

Lebih bagus:

```text
ExecutablePath
→ WHERE?

CommandLine
→ HOW?
```

CommandLine berisi informasi invocation/arguments; terkadang full path executable juga tercantum di dalamnya, tetapi bukan itu definisinya.

---

### Q5 StartName ✅

Benar.

---

### Q6 Service vs Process 🟡

Kamu masih menulis:

> service itu program yang disediakan OS
> proses itu terkait dengan service

Ini masih kurang tepat.

**Reminder yang akan terus kita ulang:**

> **Service ≠ Process**

Service adalah background component managed by Windows Service Control Manager.

Process adalah instance program yang sedang berjalan.

Contoh:

```text
Dhcp Service
     ↓
ProcessId 2300
     ↓
svchost.exe
```

---

### Q7 SHA-256 ✅

Benar.

---

### Q8 Digital Signature ✅/🟡

Kamu mengatakan:

> melihat apakah proses tersebut valid

Yang diperiksa sebenarnya **signature pada file**, bukan "process valid".

Lebih tepat:

> Digital Signature membantu memeriksa status signature/certificate dan signer information dari executable/file.

---

### Q9 Valid Signature 🟢

Kamu memahami:

> valid berarti signature tervalidasi dan bisa dilihat signer-nya.

Arah pemahaman sudah benar.

Namun:

```text
Valid
≠
Program resmi
≠
Program aman
```

`Valid` berarti signature berhasil divalidasi menurut mekanisme trust yang digunakan.

---

### Q10 ✅/🟡

Contoh yang kamu berikan tentang signed driver lama yang punya vulnerability adalah contoh yang mungkin relevan, tetapi jangan membatasi konsep ke driver.

Prinsip umumnya:

> **A valid signature does not guarantee benign intent or safe behavior.**

---

### Q11 ✅

Benar: killing process dapat menghilangkan atau mengubah evidence.

---

### Q12 ✅

```text
1424
```

Benar.

---

### Q13 ✅

Bagus.

Kamu memahami process bisa berhenti, dan kalau dijalankan lagi bisa mendapatkan PID berbeda.

Ini insight penting:

> **PID bukan permanent identity of a program.**

---

### Q14 ✅

Perbedaan:

```text
Metadata
→ file information

SHA-256
→ file fingerprint

Digital Signature
→ signature / signer / certificate information
```

Sudah bagus.

---

### Q15 ✅

> untuk melihat dengan apa saya menggunakan account

Intinya benar.

Lebih tepat:

> `whoami` menunjukkan **current user/security context**.

---

# 📊 DAY 8 SCORE

| Materi                | Status |
| --------------------- | ------ |
| `whoami`              | 🟢     |
| User context          | 🟢     |
| Process + User        | 🟢     |
| PID / PPID            | 🟢     |
| ExecutablePath        | 🟢     |
| CommandLine           | 🟢     |
| Privilege concept     | 🟢     |
| Process investigation | 🟢     |
| Service vs Process    | 🟡     |
| Valid Signature       | 🟡     |
| Evidence mindset      | 🟢     |
| Hands-on              | 🟢     |
| SOC report            | 🟢     |

# 🟢 DAY 8 — PASSED

Kamu sudah melewati standar **76%** dengan cukup jelas.

---

# 🧠 REMINDER UNTUK DAY 9

Kesalahan kecil yang akan sengaja aku masukkan lagi:

```text
1. Service ≠ Process

2. services.exe ≠ service.exe

3. ExecutablePath → WHERE?
   CommandLine → HOW?

4. User = account/security context
   bukan Process.

5. Valid Digital Signature
   ≠ Automatically Safe

6. Metadata tidak otomatis memberi Execution Time.

7. PID bukan permanent identity.
```

Ini justru akan membantu **ketelitianmu**, karena beberapa kesalahanmu bukan karena tidak mengerti konsep, tetapi karena **terminologi kurang presisi**.

---

# 💻 COMMANDS DAY 8

Ini kumpulan command yang kita pelajari hari ini.

### 1. Current User

```cmd
whoami
```

Fungsi:

```text
Current user / security context
```

---

### 2. Current User Groups

```cmd
whoami /groups
```

Fungsi:

```text
Group membership dan security context
```

---

### 3. Semua Process + User

```powershell
Get-Process -IncludeUserName |
Select-Object ProcessName, Id, UserName
```

Fungsi:

```text
ProcessName
PID
UserName
```

---

### 4. Satu Process + User

```powershell
Get-Process -Id <PID> -IncludeUserName |
Select-Object ProcessName, Id, UserName
```

Contoh:

```powershell
Get-Process -Id 6348 -IncludeUserName |
Select-Object ProcessName, Id, UserName
```

---

### 5. Satu Process + Full Context

```powershell
Get-CimInstance Win32_Process -Filter "ProcessId = <PID>" |
Select-Object Name, ProcessId, ParentProcessId, ExecutablePath, CommandLine
```

Contoh:

```powershell
Get-CimInstance Win32_Process -Filter "ProcessId = 6348" |
Select-Object Name, ProcessId, ParentProcessId, ExecutablePath, CommandLine
```

---

# 🧠 COMMAND MAP DAY 8

```text
whoami
   ↓
WHO AM I?

whoami /groups
   ↓
WHAT GROUPS / SECURITY CONTEXT?

Get-Process -IncludeUserName
   ↓
WHO IS RUNNING THE PROCESS?

Get-CimInstance Win32_Process
   ↓
PID / PPID / PATH / COMMAND
```

Jadi sekarang command toolkit kita sudah berkembang:

```text
DAY 1
hostname
whoami
systeminfo
tasklist

DAY 3
Get-CimInstance Win32_Process

DAY 4
Get-FileHash
Get-AuthenticodeSignature
Get-Item

DAY 5
Get-Service
Get-CimInstance Win32_Service
Get-Process
sc query

DAY 7
Get-CimInstance Win32_Process
    → Name
    → ProcessId
    → ParentProcessId
    → ExecutablePath
    → CommandLine

DAY 8
whoami /groups
Get-Process -IncludeUserName
```

**Day 8 selesai.** Menurut aturan kita, kita langsung boleh masuk **Day 9** pada sesi berikutnya.
